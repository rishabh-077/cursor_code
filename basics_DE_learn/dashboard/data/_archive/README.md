# Archived dashboard data

**`progress_weeks_legacy.json`** — former `progress.json` keys `week1` and `week2` (Block A/B/C, per-week LC/SQL/exit lists).

Portal + `lc_log.json` + `dsa_mastery.json` are the live system. Do not delete this file unless you are sure you will never need legacy fields.
